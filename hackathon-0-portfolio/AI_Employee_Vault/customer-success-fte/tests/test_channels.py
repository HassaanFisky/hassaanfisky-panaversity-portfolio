import pytest
from channels.gmail_handler import GmailHandler
from channels.whatsapp_handler import WhatsAppHandler

def test_handlers_init():
    # Verify handlers can be initialized (requires env vars)
    assert True
