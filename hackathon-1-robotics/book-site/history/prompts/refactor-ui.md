# Prompt: Refactor UI Component

**Goal**: Modernize and visually upgrade a specific UI component.

**Context**:

- Style: "Apple Glass" / Premium Glassmorphism
- Constraints: Must support Light & Dark mode perfectly.
- Performance: No heavy libraries, use pure CSS/JS where possible.

**Instructions**:

1. Read the attached component code.
2. Refactor it to use:
   - CSS Modules for scoping
   - Functional Component patterns (Hooks)
   - Proper prop typing (if TS)
3. Apply the project's signature "Glass" effect:
   ```css
   background: rgba(255, 255, 255, 0.12);
   backdrop-filter: blur(40px) saturate(200%);
   border: 1px solid rgba(255, 255, 255, 0.25);
   ```

**Target Component**: [INSERT COMPONENT NAME]
