# Prompt: Fix CSS / Layout Issue

**Goal**: Resolve a visual bug or layout inconsistency.

**Context**:

- Framework: Docusaurus (Infima CSS + Custom CSS)
- Issue: [DESCRIBE ISSUE]

**Instructions**:

1. Analyze the CSS hierarchy.
2. Propose a fix that:
   - Does not break responsive behavior (Mobile/Desktop).
   - Uses CSS variables for theming (dark/light mode).
   - Avoids `!important` unless absolutely necessary.
3. Provide the CSS snippet to add to `custom.css` or the module file.

**Reference**:

- `src/css/custom.css`
- `docusaurus.config.js`
