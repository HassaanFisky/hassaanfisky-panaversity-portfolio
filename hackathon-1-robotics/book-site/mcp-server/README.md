# MCP Server Stub

This directory represents the configuration for a Model Context Protocol (MCP) server integration.
It allows local LLMs to interface directly with the textbook content database.

## Config

- Port: 3000
- Protocol: SSE (Server-Sent Events)

## Capabilities

- `read_chapter`: Access chapter markdown
- `search_textbook`: Semantic search
